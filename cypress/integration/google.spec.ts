/// <reference types="Cypress" />
/* eslint-disable */
// @ts-ignore

describe('My first test', () => {
  it('compares value with true', () => {
    const value = true;
    expect(value).to.equal(true);
  })
});